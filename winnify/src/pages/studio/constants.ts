// Shared product constants for the Student Studio.

// One revision sitting. A student with 120 cards due needs a finish line, not a
// backlog — the deck is dealt in chunks of this size and Home frames its chip
// as "a session" rather than the raw due count. Kept here so the number Home
// promises and the number the deck deals can never drift apart.
export const SESSION_CARDS = 20;
